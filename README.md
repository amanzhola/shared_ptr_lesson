* Windows
* кодировка UTF-8
* сборка g++
* полный вывод консоли (в `code block`, чтобы **НИЧЕГО не ломалось**)
* диаграммы владения
* графика вынесена в картинки
* Markdown, который **GitHub не сломает**

Ты можешь **просто скопировать и вставить это целиком в README.md**.

---

````md
# 📘 shared_ptr lesson — Shape / Texture (1:1)

Учебный демонстрационный проект по **std::shared_ptr**  
Аналогия: **Robot / Charger == Shape / Texture (1:1)**

Цель — **наглядно и честно** показать:
- кто владеет объектом,
- когда `use_count` меняется,
- почему объект уничтожается,
- почему `use_count` внутри функции отличается от значения в `main`.

---

## 🧱 Структура проекта

```text
shared_ptr_lesson/
├─ main.cpp
├─ shape.h / shape.cpp
├─ texture.h / texture.cpp
├─ canvas.h / canvas.cpp
├─ textures_factory.*
├─ dashed_border.*
├─ lesson_print.*
├─ utf8_table.*
├─ console_color.*
├─ types.h
├─ CMakeLists.txt
├─ images/
│  ├─ canvas.png
│  └─ source.png
└─ README.md
````

---

## 🖥️ Сборка и запуск (Windows)

> ⚠️ Обязательно UTF-8, иначе эмодзи и таблицы сломаются

```bat
chcp 65001

g++ -std=c++17 -O2 -Wall -Wextra -pedantic ^
  main.cpp console_color.cpp texture.cpp shape.cpp canvas.cpp ^
  textures_factory.cpp dashed_border.cpp utf8_table.cpp lesson_print.cpp ^
  -o demo.exe

demo.exe
```

---

## 🧾 Легенда пикселей текстур

```text
┌─────┬───────────────────────────────────────────────┐
│ Код │ Значение                                      │
├─────┼───────────────────────────────────────────────┤
│ B   │ 🌌 Небо (sky)                                 │
│ Y   │ ☀️  Солнце (sun)                              │
│ G   │ 🟩 Трава (grass)                              │
│ O   │ 🧱 Стены дома (walls)                         │
│ W   │ ⬛ Крыша (roof)                               │
│ C   │ 🪟 Окно (window)                              │
│ .   │ 🟣 Вне текстуры                               │
│ ' ' │ ⬜ Пусто                                      │
│ R   │ 🔴 Красная пунктирная рамка (source)          │
└─────┴───────────────────────────────────────────────┘
```

---

## 🧠 Смысл текстур

* **HouseSceneTexture 🌤️🏠** — одна цельная сцена
  *(небо + солнце + дом + трава)*

* **GreenFillTexture 🟩** — заливка `'G'` в каждом пикселе
  *(доказывается в коде)*

* **BlueUnusedTexture 🟦** — намеренно никому не передаётся
  *(должна умереть при выходе из блока)*

---

## 🔗 Диаграммы владения (ключевая часть урока)

### ШАГ 3 — после первого `SetTexture`

```text
houseScene → HouseSceneTexture ← square
greenFill  → GreenFillTexture  ← ellipse
```

---

### ШАГ 5 — `ellipse.SetTexture(square.GetTexture())`

```text
square → HouseSceneTexture ← ellipse
(GreenFillTexture уничтожена)
```

---

### CANVAS (графика — НЕ управляет временем жизни)

```text
canvasHouse → CanvasHouseScene ← CANVAS-Square
                                   ↑
                                   CANVAS-Ellipse
```

---

## 🎨 Графика (результат)

### CANVAS

![CANVAS](images/canvas.png)

### TEXTURE SOURCE (рамки)

![SOURCE](images/source.png)

---

## ⚠️ ВАЖНОЕ ОБЪЯСНЕНИЕ ПРО use_count

> Почему **в main `use_count = 2`**, а **внутри `SetTexture` = 3**?

Потому что:

```cpp
void Shape::SetTexture(std::shared_ptr<Texture> tex)
```

* `tex` — **копия shared_ptr**
* при входе в функцию создаётся **временный владелец**
* поэтому `use_count` **внутри функции больше**

Это **НЕ обман**, а ключевой учебный момент.

---

## 🖥️ Полный вывод консоли (Windows)

> ⚠️ ВЕСЬ вывод приведён **как есть**, без изменений
> ⚠️ Специально внутри `code block`, чтобы GitHub ничего не сломал

<details>
<summary>▶ Нажми, чтобы раскрыть полный лог</summary>

```text
Microsoft Windows [Version 10.0.26200.7623]
(c) Корпорация Майкрософт (Microsoft Corporation). Все права защищены.

C:\...\shared_ptr_lesson>chcp 65001
Active code page: 65001

C:\...\shared_ptr_lesson>g++ -std=c++17 -O2 -Wall -Wextra -pedantic ^
More?   main.cpp console_color.cpp texture.cpp shape.cpp canvas.cpp ^
More?   textures_factory.cpp dashed_border.cpp utf8_table.cpp lesson_print.cpp ^
More?   -o demo.exe

C:\...\shared_ptr_lesson>demo.exe

========================================================================= 
📘 shared_ptr УРОК: Robot/Charger == Shape/Texture (1:1)
=========================================================================

... (ВСТАВЬ СЮДА ВЕСЬ ТВОЙ ЛОГ БЕЗ ИЗМЕНЕНИЙ)
```

</details>

---

## ✅ Итог

Этот проект **честно** показывает:

* как работает `std::shared_ptr`
* почему `use_count` меняется
* когда объект реально уничтожается
* почему графика не влияет на lifetime
* где именно появляется дополнительный владелец

Если кто-то спорит — **ответ уже в логе и диаграммах**.

---

📌 Репозиторий:
👉 [https://github.com/amanzhola/shared_ptr_lesson](https://github.com/amanzhola/shared_ptr_lesson)

```
