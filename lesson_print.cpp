#include "lesson_print.h"
#include <iostream>

void PrintLine() {
    std::cout << "--------------------------------------------------------------------------\n";
}

void PrintHeader(const std::string& title) {
    std::cout << "\n=========================================================================\n";
    std::cout << title << "\n";
    std::cout << "=========================================================================\n";
}

void PrintPixelLegend() {
    PrintHeader("🧾 ЛЕГЕНДА ПИКСЕЛЕЙ ТЕКСТУР (что означает каждая буква)");

    std::cout << "┌─────┬───────────────────────────────────────────────┐\n";
    std::cout << "│ Код │ Значение                                      │\n";
    std::cout << "├─────┼───────────────────────────────────────────────┤\n";
    std::cout << "│ B   │ 🌌 Небо (sky)                                 │\n";
    std::cout << "│ Y   │ ☀️  Солнце (sun)                              │\n";
    std::cout << "│ G   │ 🟩 Трава (grass)                              │\n";
    std::cout << "│ O   │ 🧱 Стены дома (walls)                         │\n";
    std::cout << "│ W   │ ⬛ Крыша (roof)                               │\n";
    std::cout << "│ C   │ 🪟 Окно (window)                              │\n";
    std::cout << "│ .   │ 🟣 Выход за пределы текстуры (outside texture)│\n";
    std::cout << "│ ' ' │ ⬜ Пусто (ничего не рисуем)                   │\n";
    std::cout << "│ R   │ 🔴 Красная пунктирная рамка (source-mark)     │\n";
    std::cout << "└─────┴───────────────────────────────────────────────┘\n";

    std::cout << "\n📌 Важно:\n";
    std::cout << "  • HouseSceneTexture 🌤️🏠 — это ОДНА картинка-сцена: небо(B)+солнце(Y)+дом(O/W/C)+трава(G).\n";
    std::cout << "  • GreenFillTexture 🟩 — это заливка символом 'G' на каждом пикселе (доказываем в коде).\n";
}
